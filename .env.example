
# Environment template for local development / CI
# Copy to `.env` and fill values before running E2E that require R2 or D1

# -------------------------------
# Cloudflare R2 (required for presign/upload E2E)
# -------------------------------
R2_BUCKET=
CLOUDFLARE_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
# Optional: custom endpoint (e.g. https://<account>.r2.cloudflarestorage.com)
R2_ENDPOINT=


# -------------------------------
# Cloudflare D1 / Wrangler bindings
# -------------------------------
# The binding name should match the name in `wrangler.toml` (e.g. "tsumi_meshi_db").
# Used by local wrangler dev and the Worker runtime.
D1_BINDING_NAME=tsumi_meshi_db


# -------------------------------
# Auth / Tokens
# -------------------------------
# JWT secret used by local API (used by tests / local server)
# IMPORTANT: replace the default value in any non-dev environment.
JWT_SECRET=change_me_local_secret

# Optional: token for Cloudflare API usage in CI (wrangler publish, d1 uploads)
# Provide a scoped API token for CI instead of embedding account credentials.
CLOUDFLARE_API_TOKEN=


# -------------------------------
# App / Public config
# -------------------------------
# Optional: node environment
NODE_ENV=development

# Public API base (used by `app/utils/api/client.ts` when calling external API services)
# For production point this to your API host including API base path (recommended):
#   https://api.example.com/api/v1
# Example for wrangler dev: http://localhost:8787/api/v1
NUXT_PUBLIC_API_BASE=http://localhost:8787/api/v1


# -------------------------------
# Observability / Optional services
# -------------------------------
# Sentry DSN (optional)
SENTRY_DSN=


# -------------------------------
# Security notes
# -------------------------------
# - Never commit real secrets. Copy this file to `.env` and populate values.
# - Ensure `.gitignore` contains `.env`, `.wrangler/`, and `.d1/` to avoid accidental commits.
# - Replace `JWT_SECRET` with a strong value in staging/production.
