{
  "name": "@tsumi-meshi/workers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev --config ./wrangler.toml",
    "build": "echo \"No build step; wrangler handles TS during dev/deploy\"",
    "deploy": "wrangler publish --config ./wrangler.toml",
    "test": "vitest"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.948.0",
    "@aws-sdk/s3-request-presigner": "^3.948.0",
    "hono": "^4.11.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251213.0",
    "miniflare": "^4.20251210.0",
    "wrangler": "^4.54.0"
  }
}
